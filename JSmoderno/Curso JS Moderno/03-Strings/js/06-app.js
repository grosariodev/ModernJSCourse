const producto = "Monitor 24 pulgadas";


// .repeat es para repetir un string
const text = ' en promocion'.repeat(3);

console.log(text);
console.log(`${producto}${text}`)

// Split es para dividir un string
const actividad = "Estoy aprendiendo JS moderno";
console.log(actividad.split(" "))


// Ejemplo de split:

const receta = "Pollo, Arroz, Papa, Queso";
console.log(receta.split(", "));