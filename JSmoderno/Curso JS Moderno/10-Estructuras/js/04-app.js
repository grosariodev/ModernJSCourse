// Operador Mayor que y Menor que

const dinreo = 1000;
const totalApagar = 500;
const tarjeta = true;
const cheque = true;


if(dinreo >= totalApagar) {
    console.log('Si podemos pagar');
} else if(tarjeta) {
    console.log('Si puedo pagar porque tengo la tarjeta');
} else if(tarjeta) {
    console.log('Si puedo pagar porque tengo la tarjeta');
} else {
    console.log('Fondos insuficientes');
}