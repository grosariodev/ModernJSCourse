// A diferencia de querySelector, querySelectorAll selecciona todos los elementos llamados por el nombre que tengan, y los almacena en nodos.

const card = document.querySelectorAll('.card');
console.log(card)


const formulario = document.querySelectorAll('#formulario');
console.log(formulario);